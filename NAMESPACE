useDynLib(SSPA)

importClassesFrom(methods,
	          character,
                  list,
                  logical,
                  missing,
                  numeric)

importMethodsFrom(methods,
                  show)

importFrom(graphics,
           abline,
           arrows,           
           legend,
           matlines,
           matplot,
           plot,
           points,
           text)

importFrom(limma, 
           convest)

importFrom(lattice,
	   trellis.par.get, 
	   histogram, 
           xyplot,
	   qqmath,
	   panel.histogram,
	   panel.mathdensity,
           panel.xyplot,
           panel.abline,
           panel.qqmathline,
           panel.qqmath,
           prepanel.qqmathline)

importFrom(methods, 
           new)

importFrom(stats,
           fft,
           approx,
           optimize,
           dbinom,
           dnorm,
           pnorm,
           qnorm,  
           dt,
           pt,
           qt)  

export(pilotData, sampleSize, power)
exportMethods(plot, show)
exportMethods(statistics, effectivesamplesize, dof, distribution, pvalues)
exportMethods(pi0, lambda, theta)


